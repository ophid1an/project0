<div class="container">

    <div>
        <a class="button" href="/">{{__ 'back'}}</a>
    </div>

    <div class="align-center">
        <h3>{{__ 'gameStatisticsHeader'}}</h3>
    </div>

    <div class="align-center game-statistics">
        <p>{{__ 'difficultyLevel'}}:</p>
        <p>{{diff}}</p>
        <p>{{__ 'whitesC'}}:</p>
        <p>{{whitesC}}</p>
        <p>{{__ 'lettersYouFound'}}:</p>
        <p>{{thisLetters}}</p>
        {{#if otherUsername}}
            <p>{{__ 'lettersOtherFound'}} &lt;{{otherUsername}}&gt;:</p>
            <p>{{otherLetters}}</p>
        {{/if}}
    </div>

</div>
